{
  "name": "elephant-pod",
  "private": true,
  "version": "0.3.0",
  "description": "Local-first, sync-capable podcast app for web, desktop, iOS, and Android via Tauri.",
  "type": "module",
  "workspaces": [
    "apps/web",
    "apps/server"
  ],
  "scripts": {
    "dev:web": "npm --workspace @elephant-pod/web run dev",
    "dev:server": "npm --workspace @elephant-pod/server run dev",
    "build:web": "npm --workspace @elephant-pod/web run build",
    "build:server": "npm --workspace @elephant-pod/server run build",
    "build": "npm run build:web && npm run build:server",
    "test": "npm --workspace @elephant-pod/server run test && npm --workspace @elephant-pod/web run test",
    "preview:web": "npm --workspace @elephant-pod/web run preview",
    "start:server": "npm --workspace @elephant-pod/server run start",
    "typecheck": "npm --workspace @elephant-pod/web run typecheck && npm --workspace @elephant-pod/server run typecheck",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "docker:build": "docker build -f apps/server/Dockerfile -t elephant-pod:local .",
    "docker:run": "docker run --env-file .env -p 8787:8787 elephant-pod:local",
    "supabase:start": "cd infra/supabase && docker compose up -d",
    "supabase:stop": "cd infra/supabase && docker compose down",
    "supabase:logs": "cd infra/supabase && docker compose logs -f --tail=100"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  },
  "engines": {
    "node": ">=22"
  }
}
